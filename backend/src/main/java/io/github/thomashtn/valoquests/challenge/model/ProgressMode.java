package io.github.thomashtn.valoquests.challenge.model;

/**
 * Defines the supported progress mode values.
 */
public enum ProgressMode {
    SUM, DISTINCT_COUNT, MAX_GROUP, ALL, COUNT_MATCHES, RATIO, MAX_STREAK
}
