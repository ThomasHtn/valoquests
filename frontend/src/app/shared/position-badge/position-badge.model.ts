/**
 * Supported position badge sizes: the compact treatment used in the podium's "rest of the field"
 * list, and the default used everywhere a ranking position appears in a table or card row.
 */
export type PositionBadgeSize = 'sm' | 'md';
