import { Environment } from './environment.model';

/**
 * Production configuration, used by every build configuration except `development`.
 */
export const environment: Environment = {
  apiBaseUrl: '/api',
};
